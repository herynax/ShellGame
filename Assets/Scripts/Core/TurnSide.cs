namespace ShellGame.Core
{
    /// <summary>
    /// Чья сейчас инициатива в дуэли. Согласно ГДД: успешный выбор наперстка
    /// со меткой наносит урон ДРУГОЙ стороне и инициатива остаётся текущей
    /// стороне; промах передаёт инициативу оппоненту.
    /// </summary>
    public enum TurnSide
    {
        Player,
        Enemy,
    }
}
